include header.pug

h1 Velkommen, #{username}

h2 Dine chats
// We leave this empty because load.js fills it
ul#chatList
  li Indlæser chats...

if niveau === 3
  hr
  h2 Administer brugere
  ul#userList
    li Indlæser brugere...

h2 Opret ny chat

form(id="createChatForm")
  input(type='text', name='chatName', placeholder='Indtast chatnavn', required)
  button(type='submit') Opret chat

script(src='/assets/javaScript/deleteScript.js')

script(src='/assets/javaScript/load.js')
script(src='/assets/javaScript/Create.js')