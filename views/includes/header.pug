doctype html
html(lang="da")
  head
    meta(charset="UTF-8")
    title= title
    link(rel="stylesheet", href="/assets/css/main.css")
  body

    header
      div.header-left
        a(href="/") Hjem

      if username
        div.header-right
          p.badge Level #{level}
          p #{username}
          a.btn-logout(href="/logout") Log ud